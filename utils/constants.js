const AUTH_ERROR = 'Authorization error';

const DELETION_NOT_ALLOWED = 'Not allowed to remove other users movies.';

const EMAIL_NOT_UNIQUE = 'A user with this email already exists.';

const INVALID_ID = 'This id is invalid.';
const INVALID_EMAIL = 'Enter a valid email';
const INVALID_CREDENTIALS = 'Email or pasword is incorrect.';
const INVALID_URL = 'Enter a valid URL';
const INVALID_URL_OR_METHOD = 'Invalid URL or request method.';

const MIN_2_CHARACTERS = 'Minimum 2 characters.';
const MAX_30_CHARACTERS = 'Maximum 30 characters.';

const MOVIE_NOT_FOUND = 'A movie with this id does not exist.';

const SERVER_ERROR = 'Server error.';

const USER_NOT_FOUND = 'A user with this id does not exist.';

module.exports = {
  AUTH_ERROR,
  USER_NOT_FOUND,
  INVALID_ID,
  EMAIL_NOT_UNIQUE,
  DELETION_NOT_ALLOWED,
  MOVIE_NOT_FOUND,
  SERVER_ERROR,
  INVALID_EMAIL,
  INVALID_CREDENTIALS,
  INVALID_URL,
  MIN_2_CHARACTERS,
  MAX_30_CHARACTERS,
  INVALID_URL_OR_METHOD,
};
